<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LactoseFree" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/LightPrimaryColor</item>
        <item name="colorPrimaryDark">@color/DarkPrimaryColor</item>
        <item name="colorAccent">@color/AccentColor</item>
        <!-- Background Color. -->
        <item name="android:windowBackground">@color/LightPrimaryColor</item>
        <!-- Font. -->
        <item name="editTextStyle">@style/customEditTextFontStyle</item>
        <item name="android:textViewStyle">@style/customTextViewFontStyle</item>
        <item name="titleTextStyle">@style/customToolbarFontStyle</item>

    </style>

    <style name="Theme.LactoseFree" parent="Base.Theme.LactoseFree" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/LightPrimaryColor</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.LactoseFree</item>
    </style>

</resources>