<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LactoseFree" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/LightPrimaryColor</item>
        <item name="colorPrimaryDark">@color/DarkPrimaryColor</item>
        <item name="colorAccent">@color/AccentColor</item>
        <!-- Background Color. -->
        <item name="android:windowBackground">@color/LightPrimaryColor</item>
    </style>

    <style name="Theme.LactoseFree" parent="Base.Theme.LactoseFree" />
</resources>